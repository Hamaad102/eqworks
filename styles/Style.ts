const TitleStyle = { color: 'black', fontWeight: 'bold', fontSize: 50 }

const ButtonStyle = {
	color: 'rgba(0, 0, 0, 0.87)',
	borderColor: 'rgba(0, 0, 0, 0.87)',
	height: 55,
}

const barColor: string = '#DD2D4A'
const axisLabelColor: string = '#FDECEF'
const tickLabelColor: string = '#2E4057'

const margin = 32
const defaultWidth = 100
const defaultHeight = 100

export {
	TitleStyle,
	ButtonStyle,
	barColor,
	axisLabelColor,
	tickLabelColor,
	margin,
	defaultWidth,
	defaultHeight,
}
